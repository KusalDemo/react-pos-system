export class Customer{
    name: string
    email: string
    address: string
    salary: number
    constructor(name: string, email: string, address: string, salary: number) {
        this.name = name
        this.email = email
        this.address = address
        this.salary = salary
    }
}